var core = function(app){

    if(typeof app == "undefined"){
        throw new Error("App in undefined");
    }

    var _this = this;

    this._app = app;

    this.router = function(){
        var router = require('./router');

        return router(_this._app);
    }

    return this;
};

module.exports = core;